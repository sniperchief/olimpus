ANTHROPIC_API_KEY=
PORT=8080
MODEL_ID=claude-haiku-4-5

# Optional — override for deployment (defaults are fine for local dev):
# DB_PATH=data/olimpus.db   
   # e.g. /data/olimpus.db when mounted on a persistent volume
# PUBLIC_URL=                  # e.g. https://your-app.up.railway.app/ — advertised in the Agent Card and used by the bridge script to reach the deployed engine
